#include <stdio>
/** main - fuctions master
 * Return: Always 0
 */

 int.main(void)
 int _isupper(int c) {
    if (c >= 'A' && c <= 'Z') {
        return 1;  // uppercase character
    } else {
        return 0;  // not uppercase character
    }
}
